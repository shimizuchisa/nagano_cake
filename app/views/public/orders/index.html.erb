<body>
  <%= render 'layouts/public_header_template' %>
  <main>
    <div class="container">
      <h1 class="mb-5">ご注文一覧</h1>
      <div class="row">
        <div class="col-11">
          <div class="order_field mb-3">
            <table class="table">
              <thead>
                <tr>
                  <th>ご注文日時</th>
                  <th>お届け先</th>
                  <th>ご請求金額</th>
                  <th>ステータス</th>
                  <th></th>
                </tr>
              </thead>
              <tbody>
                <% @orders.each do |order| %>
                <tr>
                  <td claas="w-20"><%= order.created_at %></td>
                  <td class="w-50"><%= order.address_display %></td>
                  <td claas="w-10"><%= order.grand_total %></td>
                  <td claas="w-10"><%= order.order_status %></td>
                  <td claas="w-10"><%= link_to "詳細", order_path(order), class: "btn btn-light" %></td>
                </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </main>
  <%= render 'layouts/footer_template' %>
</body>